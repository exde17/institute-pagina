---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="PQR - Instituto Fundación" 
  description="Peticiones, Quejas y Reclamos - Estamos para escucharte"
>
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-600 via-blue-700 to-blue-900 text-white py-20">
    <div class="mx-auto max-w-7xl px-6 md:px-8">
      <div class="text-center max-w-3xl mx-auto">
        <div class="inline-block px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-semibold mb-6">
          Estamos Para Escucharte
        </div>
        <h1 class="text-4xl md:text-6xl font-bold mb-6">
          Peticiones, Quejas y Reclamos
        </h1>
        <p class="text-lg md:text-xl text-white/90 leading-relaxed">
          Tu opinión es muy importante para nosotros. Comparte tus inquietudes, sugerencias o felicitaciones.
        </p>
      </div>
    </div>
  </section>

  <!-- Formulario PQR -->
  <section class="py-20 bg-slate-50">
    <div class="mx-auto max-w-4xl px-6 md:px-8">
      <div class="bg-white rounded-2xl shadow-xl p-8 md:p-12">
        <h2 class="text-3xl font-bold text-slate-900 mb-6">Formulario PQR</h2>
        <p class="text-slate-600 mb-8">
          Por favor completa el siguiente formulario. Recibiremos tu mensaje y te responderemos a la brevedad posible.
        </p>

        <form id="pqr-form" class="space-y-6">
          <!-- Tipo de Solicitud -->
          <div>
            <label for="tipo" class="block text-sm font-semibold text-slate-700 mb-2">
              Tipo de Solicitud <span class="text-red-500">*</span>
            </label>
            <select 
              id="tipo" 
              name="tipo" 
              required
              class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
            >
              <option value="">Selecciona una opción</option>
              <option value="Petición">Petición</option>
              <option value="Queja">Queja</option>
              <option value="Reclamo">Reclamo</option>
              <option value="Sugerencia">Sugerencia</option>
              <option value="Felicitación">Felicitación</option>
            </select>
          </div>

          <!-- Información Personal -->
          <div class="grid md:grid-cols-2 gap-6">
            <div>
              <label for="nombre" class="block text-sm font-semibold text-slate-700 mb-2">
                Nombre Completo <span class="text-red-500">*</span>
              </label>
              <input 
                type="text" 
                id="nombre" 
                name="nombre" 
                required
                placeholder="Tu nombre completo"
                class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-semibold text-slate-700 mb-2">
                Correo Electrónico <span class="text-red-500">*</span>
              </label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                required
                placeholder="tu@email.com"
                class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
              />
            </div>
          </div>

          <div class="grid md:grid-cols-2 gap-6">
            <div>
              <label for="telefono" class="block text-sm font-semibold text-slate-700 mb-2">
                Teléfono <span class="text-red-500">*</span>
              </label>
              <input 
                type="tel" 
                id="telefono" 
                name="telefono" 
                required
                placeholder="+57 300 123 4567"
                class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
              />
            </div>

            <div>
              <label for="ciudad" class="block text-sm font-semibold text-slate-700 mb-2">
                Ciudad
              </label>
              <input 
                type="text" 
                id="ciudad" 
                name="ciudad"
                placeholder="Tu ciudad"
                class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
              />
            </div>
          </div>

          <!-- Asunto -->
          <div>
            <label for="asunto" class="block text-sm font-semibold text-slate-700 mb-2">
              Asunto <span class="text-red-500">*</span>
            </label>
            <input 
              type="text" 
              id="asunto" 
              name="asunto" 
              required
              placeholder="Describe brevemente el asunto"
              class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none"
            />
          </div>

          <!-- Mensaje -->
          <div>
            <label for="mensaje" class="block text-sm font-semibold text-slate-700 mb-2">
              Mensaje <span class="text-red-500">*</span>
            </label>
            <textarea 
              id="mensaje" 
              name="mensaje" 
              required
              rows="6"
              placeholder="Describe detalladamente tu petición, queja, reclamo o sugerencia..."
              class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all outline-none resize-none"
            ></textarea>
          </div>

          <!-- Botón de envío -->
          <div class="flex flex-col sm:flex-row gap-4 items-center justify-between pt-4">
            <p class="text-sm text-slate-500">
              <span class="text-red-500">*</span> Campos obligatorios
            </p>
            <button 
              type="submit" 
              id="submit-btn"
              class="w-full sm:w-auto bg-gradient-to-r from-blue-600 to-blue-700 text-white px-10 py-4 rounded-xl font-bold shadow-lg hover:shadow-xl transform hover:scale-105 transition-all disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none"
            >
              <span id="btn-text">Enviar PQR</span>
              <span id="btn-loading" class="hidden">Enviando...</span>
            </button>
          </div>
        </form>

        <!-- Mensaje de éxito -->
        <div id="success-message" class="hidden mt-6 p-4 bg-green-50 border-2 border-green-200 rounded-lg">
          <div class="flex items-center gap-3">
            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <div>
              <h4 class="font-bold text-green-900">¡Mensaje enviado exitosamente!</h4>
              <p class="text-sm text-green-700">Hemos recibido tu PQR. Te responderemos pronto.</p>
            </div>
          </div>
        </div>

        <!-- Mensaje de error -->
        <div id="error-message" class="hidden mt-6 p-4 bg-red-50 border-2 border-red-200 rounded-lg">
          <div class="flex items-center gap-3">
            <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
            <div>
              <h4 class="font-bold text-red-900">Error al enviar</h4>
              <p class="text-sm text-red-700">Por favor intenta de nuevo o contáctanos directamente.</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Información de contacto alternativa -->
      <div class="mt-12 grid md:grid-cols-3 gap-6">
        <div class="bg-white rounded-xl p-6 shadow-md text-center">
          <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
          </div>
          <h3 class="font-bold text-slate-900 mb-2">Correo Electrónico</h3>
          <p class="text-sm text-slate-600">aprenderbn@gmail.com</p>
        </div>

        <div class="bg-white rounded-xl p-6 shadow-md text-center">
          <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6 text-green-600" fill="currentColor" viewBox="0 0 24 24">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>
            </svg>
          </div>
          <h3 class="font-bold text-slate-900 mb-2">WhatsApp</h3>
          <p class="text-sm text-slate-600">Próximamente</p>
        </div>

        <div class="bg-white rounded-xl p-6 shadow-md text-center">
          <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <h3 class="font-bold text-slate-900 mb-2">Horario de Atención</h3>
          <p class="text-sm text-slate-600">Lun - Vie: 8:00 AM - 5:00 PM</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  const form = document.getElementById('pqr-form') as HTMLFormElement;
  const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
  const btnText = document.getElementById('btn-text') as HTMLSpanElement;
  const btnLoading = document.getElementById('btn-loading') as HTMLSpanElement;
  const successMessage = document.getElementById('success-message') as HTMLDivElement;
  const errorMessage = document.getElementById('error-message') as HTMLDivElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    // Ocultar mensajes previos
    successMessage.classList.add('hidden');
    errorMessage.classList.add('hidden');
    
    // Deshabilitar botón y mostrar loading
    submitBtn.disabled = true;
    btnText.classList.add('hidden');
    btnLoading.classList.remove('hidden');

    // Recopilar datos del formulario
    const formData = new FormData(form);
    const data = {
      tipo: formData.get('tipo'),
      nombre: formData.get('nombre'),
      email: formData.get('email'),
      telefono: formData.get('telefono'),
      ciudad: formData.get('ciudad') || 'No especificada',
      asunto: formData.get('asunto'),
      mensaje: formData.get('mensaje'),
      fecha: new Date().toLocaleString('es-CO', { timeZone: 'America/Bogota' })
    };

    try {
      // Enviar a FormSubmit.co (servicio gratuito de envío de emails)
      const response = await fetch('https://formsubmit.co/aprenderbn@gmail.com', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json'
        },
        body: JSON.stringify({
          _subject: `PQR - ${data.tipo}: ${data.asunto}`,
          _template: 'table',
          _captcha: false,
          ...data
        })
      });

      if (response.ok) {
        // Mostrar mensaje de éxito
        successMessage.classList.remove('hidden');
        form.reset();
        
        // Scroll al mensaje de éxito
        successMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });
      } else {
        throw new Error('Error en el envío');
      }
    } catch (error) {
      // Mostrar mensaje de error
      errorMessage.classList.remove('hidden');
      errorMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });
    } finally {
      // Rehabilitar botón
      submitBtn.disabled = false;
      btnText.classList.remove('hidden');
      btnLoading.classList.add('hidden');
    }
  });
</script>
