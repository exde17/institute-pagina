---
import Layout from '../layouts/Layout.astro';
---
<Layout title="FCM INSTITUTE — Educación y Desarrollo">
  <!-- Carrusel Hero -->
  <section class="relative overflow-hidden h-[600px] md:h-[700px]">
    <!-- Carrusel de imágenes -->
    <div id="hero-carousel" class="absolute inset-0">
      <div class="carousel-item active">
        <img src="/img/equipo/PR-1.jpg" alt="Equipo" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/equipo/PR-11.jpg" alt="Equipo" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/equipo/PR-18.jpg" alt="Equipo" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/instalaciones/PR-12.jpg" alt="Instalaciones" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/instalaciones/PR-9.jpg" alt="Instalaciones" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/instalaciones/PR-7.jpg" alt="Instalaciones" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/instalaciones/PR-5.jpg" alt="Instalaciones" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/instalaciones/PR-3.jpg" alt="Instalaciones" class="absolute inset-0 h-full w-full object-cover" />
      </div>
      <div class="carousel-item">
        <img src="/img/instalaciones/PR-1.jpg" alt="Instalaciones" class="absolute inset-0 h-full w-full object-cover" />
      </div>
    </div>
    
    <!-- Overlay oscuro para legibilidad -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-900/70 via-blue-800/60 to-purple-900/70"></div>
    
    <!-- Contenido sobre el carrusel -->
    <div class="relative mx-auto max-w-7xl px-6 md:px-8 h-full flex items-center">
      <div class="max-w-3xl text-white">
        <div class="inline-block mb-6">
          <span class="inline-flex items-center gap-2 px-5 py-2.5 bg-white/20 backdrop-blur-sm text-white rounded-full text-sm font-semibold border border-white/30">
            <i class="fa-solid fa-star text-amber-300"></i>
            Institución de educación superior
          </span>
        </div>
        
        <h1 class="text-4xl md:text-6xl font-bold leading-tight mb-6">
          Educación y desarrollo para transformar comunidades
        </h1>
        
        <p class="text-lg md:text-xl text-white/90 max-w-2xl leading-relaxed mb-8">
          Formación académica de excelencia, programas de empleabilidad y compromiso social para construir un futuro mejor.
        </p>
        
        <div class="flex flex-wrap gap-4">
          <a href="/programas" class="bg-white text-blue-900 px-8 py-4 rounded-lg font-bold shadow-xl hover:shadow-2xl transform hover:scale-105 transition-all duration-300 inline-flex items-center gap-2">
            Ver programas académicos
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
            </svg>
          </a>
          <a href="/nosotros" class="bg-white/10 backdrop-blur-sm text-white px-8 py-4 rounded-lg font-bold border-2 border-white/30 hover:bg-white/20 transition-all duration-300">
            Conócenos
          </a>
        </div>

        <!-- Estadísticas institucionales -->
        <div class="mt-12 grid grid-cols-3 gap-8 pt-8 border-t border-white/20">
          <div>
            <div class="text-3xl md:text-4xl font-bold counter" data-target="500">0</div>
            <div class="text-sm text-white/80 mt-1">Estudiantes</div>
          </div>
          <div>
            <div class="text-3xl md:text-4xl font-bold counter" data-target="120">0</div>
            <div class="text-sm text-white/80 mt-1">Familias apoyadas</div>
          </div>
          <div>
            <div class="text-3xl md:text-4xl font-bold counter" data-target="15">0</div>
            <div class="text-sm text-white/80 mt-1">Programas</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Controles del carrusel -->
    <button id="prev-btn" class="absolute left-4 top-1/2 -translate-y-1/2 bg-white/20 backdrop-blur-sm hover:bg-white/30 text-white p-3 rounded-full transition-all z-10">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
    </button>
    <button id="next-btn" class="absolute right-4 top-1/2 -translate-y-1/2 bg-white/20 backdrop-blur-sm hover:bg-white/30 text-white p-3 rounded-full transition-all z-10">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
      </svg>
    </button>

    <!-- Indicadores -->
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 flex gap-2 z-10">
      <button class="carousel-indicator active w-2 h-2 rounded-full bg-white" data-index="0"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="1"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="2"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="3"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="4"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="5"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="6"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="7"></button>
      <button class="carousel-indicator w-2 h-2 rounded-full bg-white/50" data-index="8"></button>
    </div>
  </section>

  <!-- Sección de Programas con ancho consistente -->
  <section class="mx-auto max-w-7xl px-6 md:px-8 py-20">
    <div class="text-center mb-16">
      <div class="inline-block px-4 py-2 bg-blue-50 text-blue-700 rounded-full text-sm font-semibold mb-4">
        Formación académica
      </div>
      <h2 class="text-3xl md:text-5xl font-bold text-slate-900 mb-4">
        Programas destacados
      </h2>
      <p class="text-lg text-slate-600 max-w-2xl mx-auto mb-8">
        Descubre nuestros programas técnicos y profesionales diseñados para tu desarrollo
      </p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-8 mb-12">
      <!-- Marketing y Redes Sociales -->
      <a href="/programas#marketing" class="group relative block rounded-2xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:scale-105">
        <div class="aspect-[3/4] overflow-hidden">
          <img src="/img/flyers/MARKETING Y REDES SOCIALES.png" alt="Marketing y Redes Sociales" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
        </div>
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute bottom-0 left-0 right-0 p-6 text-white">
            <h3 class="text-2xl font-bold mb-2">Marketing y Redes Sociales</h3>
            <p class="text-sm text-white/90 mb-4">Aprende estrategias digitales, gestión de redes y marketing de contenidos para impulsar marcas en el entorno digital.</p>
            <span class="inline-flex items-center gap-2 bg-white text-blue-900 px-4 py-2 rounded-lg font-semibold text-sm">
              Ver detalles
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </span>
          </div>
        </div>
      </a>

      <!-- Gestión de la Innovación -->
      <a href="/programas#innovacion" class="group relative block rounded-2xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:scale-105">
        <div class="aspect-[3/4] overflow-hidden">
          <img src="/img/flyers/GESTIÓN DE LA INNOVACIÓN.png" alt="Gestión de la Innovación" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
        </div>
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute bottom-0 left-0 right-0 p-6 text-white">
            <h3 class="text-2xl font-bold mb-2">Gestión de la Innovación</h3>
            <p class="text-sm text-white/90 mb-4">Desarrolla capacidades para liderar proyectos innovadores, gestionar el cambio y crear soluciones creativas en las organizaciones.</p>
            <span class="inline-flex items-center gap-2 bg-white text-purple-900 px-4 py-2 rounded-lg font-semibold text-sm">
              Ver detalles
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </span>
          </div>
        </div>
      </a>

      <!-- Sistemas Fotovoltaicos -->
      <a href="/programas#fotovoltaicos" class="group relative block rounded-2xl overflow-hidden shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:scale-105">
        <div class="aspect-[3/4] overflow-hidden">
          <img src="/img/flyers/FOTOVOLTAICOS.png" alt="Sistemas Fotovoltaicos" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
        </div>
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute bottom-0 left-0 right-0 p-6 text-white">
            <h3 class="text-2xl font-bold mb-2">Sistemas Fotovoltaicos</h3>
            <p class="text-sm text-white/90 mb-4">Especialízate en energías renovables, instalación y mantenimiento de paneles solares para un futuro sostenible.</p>
            <span class="inline-flex items-center gap-2 bg-white text-green-900 px-4 py-2 rounded-lg font-semibold text-sm">
              Ver detalles
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </span>
          </div>
        </div>
      </a>
    </div>

    <!-- Botón para ver todos los programas -->
    <div class="text-center">
      <a href="/programas" class="inline-flex items-center gap-3 bg-gradient-to-r from-blue-600 to-blue-800 text-white px-10 py-4 rounded-xl font-bold shadow-xl hover:shadow-2xl transform hover:scale-105 transition-all duration-300">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
        </svg>
        Ver todos los programas
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
        </svg>
      </a>
    </div>
  </section>

  <!-- Sección de impacto institucional -->
  <section class="bg-slate-50 py-20">
    <div class="mx-auto max-w-7xl px-6 md:px-8">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <div>
          <div class="inline-block px-4 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-semibold mb-6">
            Nuestro impacto
          </div>
          <h2 class="text-3xl md:text-5xl font-bold mb-6 text-slate-900">
            Transformando vidas y comunidades
          </h2>
          <p class="text-lg text-slate-600 leading-relaxed mb-8">
            Trabajamos cada día para crear oportunidades reales de crecimiento y desarrollo. Nuestros programas han transformado la vida de cientos de familias en Colombia.
          </p>
          <div class="space-y-6">
            <div class="flex items-start gap-4">
              <i class="fa-solid fa-graduation-cap text-3xl text-blue-600 flex-shrink-0 mt-1"></i>
              <div>
                <h3 class="font-bold text-xl mb-2 text-slate-900">Educación de excelencia</h3>
                <p class="text-slate-600">Formación académica de alto nivel con acompañamiento personalizado</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <i class="fa-solid fa-briefcase text-3xl text-blue-600 flex-shrink-0 mt-1"></i>
              <div>
                <h3 class="font-bold text-xl mb-2 text-slate-900">Oportunidades laborales</h3>
                <p class="text-slate-600">Conexión directa con empresas líderes del sector</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <i class="fa-solid fa-heart text-3xl text-blue-600 flex-shrink-0 mt-1"></i>
              <div>
                <h3 class="font-bold text-xl mb-2 text-slate-900">Compromiso social</h3>
                <p class="text-slate-600">Apoyo integral y desarrollo comunitario sostenible</p>
              </div>
            </div>
          </div>
          <a href="/nosotros" class="mt-10 inline-flex items-center gap-2 bg-gradient-to-r from-blue-600 to-blue-700 text-white px-8 py-4 rounded-lg font-bold hover:from-blue-700 hover:to-blue-800 transition-all shadow-lg hover:shadow-xl transform hover:scale-105">
            Conócenos mejor
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
            </svg>
          </a>
        </div>
        <div class="relative">
          <div class="aspect-video rounded-2xl shadow-2xl overflow-hidden border-4 border-white">
            <video 
              class="w-full h-full object-cover" 
              autoplay 
              muted 
              loop 
              playsinline 
              controls
            >
              <source src="img/video/Institucional.mp4" type="video/mp4" />
              Tu navegador no soporta el elemento de video.
            </video>
          </div>
          <div class="absolute -top-6 -right-6 bg-gradient-to-br from-amber-400 to-orange-500 text-white px-8 py-4 rounded-xl shadow-2xl border-4 border-white">
            <div class="font-bold text-2xl">+500</div>
            <div class="text-sm font-semibold">Vidas transformadas</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Últimas noticias con ancho consistente -->
  <section class="mx-auto max-w-7xl px-6 md:px-8 py-20">
    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-12">
      <div>
        <div class="inline-block px-4 py-2 bg-blue-50 text-blue-700 rounded-full text-sm font-semibold mb-4">
          Actualidad
        </div>
        <h2 class="text-3xl md:text-5xl font-bold text-slate-900 mb-2">
          Últimas noticias
        </h2>
        <p class="text-lg text-slate-600">Mantente informado sobre nuestras actividades</p>
      </div>
      <a href="/noticias" class="hidden md:inline-flex items-center gap-2 text-blue-700 font-semibold hover:gap-3 transition-all">
        Ver todas
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
        </svg>
      </a>
    </div>
    <div id="noticias-preview" class="grid sm:grid-cols-2 md:grid-cols-3 gap-6">
      <div class="text-center py-12 col-span-full">
        <div class="inline-block animate-spin rounded-full h-12 w-12 border-b-2 border-blue-800"></div>
        <p class="mt-4 text-slate-600">Cargando noticias...</p>
      </div>
    </div>
  </section>

  <!-- Call to action final - Opción 4: Diseño con cards flotantes -->
  <section class="bg-gradient-to-b from-slate-50 to-white py-20">
    <div class="mx-auto max-w-7xl px-6 md:px-8">
      <!-- Header centrado -->
      <div class="text-center mb-12">
        <div class="inline-block px-5 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-semibold mb-4">
          Únete a nosotros
        </div>
        <h2 class="text-3xl md:text-5xl font-bold mb-4 text-slate-900">
          ¿Listo para ser parte del cambio?
        </h2>
        <p class="text-lg text-slate-600 max-w-2xl mx-auto">
          Tu apoyo puede transformar vidas. Únete a nuestra misión de crear oportunidades reales de progreso.
        </p>
      </div>
      
      <!-- Cards flotantes -->
      <div class="grid md:grid-cols-3 gap-6">
        <!-- Card 1: Donación -->
        <div class="group bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-blue-100 hover:border-blue-300 transform hover:-translate-y-2">
          <div class="w-16 h-16 bg-gradient-to-br from-amber-400 to-orange-500 rounded-2xl flex items-center justify-center mb-6 shadow-lg group-hover:scale-110 transition-transform">
            <i class="fa-solid fa-hand-holding-heart text-white text-3xl"></i>
          </div>
          <h3 class="text-2xl font-bold text-slate-900 mb-3">Hacer una donación</h3>
          <p class="text-slate-600 mb-6 leading-relaxed">
            Contribuye directamente al futuro de jóvenes y familias. Cada aporte hace la diferencia.
          </p>
          <a href="/donar" class="inline-flex items-center gap-2 text-blue-700 font-bold group-hover:gap-3 transition-all">
            Donar ahora
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
        
        <!-- Card 2: Contacto -->
        <div class="group bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-slate-100 hover:border-blue-300 transform hover:-translate-y-2">
          <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-700 rounded-2xl flex items-center justify-center mb-6 shadow-lg group-hover:scale-110 transition-transform">
            <i class="fa-solid fa-envelope text-white text-3xl"></i>
          </div>
          <h3 class="text-2xl font-bold text-slate-900 mb-3">Contáctanos</h3>
          <p class="text-slate-600 mb-6 leading-relaxed">
            ¿Tienes preguntas? Estamos aquí para ayudarte. Conversemos sobre cómo puedes apoyar.
          </p>
          <a href="/contacto" class="inline-flex items-center gap-2 text-blue-700 font-bold group-hover:gap-3 transition-all">
            Escribir mensaje
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
        
        <!-- Card 3: Conocer más -->
        <div class="group bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-slate-100 hover:border-blue-300 transform hover:-translate-y-2">
          <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-purple-700 rounded-2xl flex items-center justify-center mb-6 shadow-lg group-hover:scale-110 transition-transform">
            <i class="fa-solid fa-circle-info text-white text-3xl"></i>
          </div>
          <h3 class="text-2xl font-bold text-slate-900 mb-3">Conoce más</h3>
          <p class="text-slate-600 mb-6 leading-relaxed">
            Descubre nuestra historia, misión y el impacto que generamos en la comunidad.
          </p>
          <a href="/nosotros" class="inline-flex items-center gap-2 text-blue-700 font-bold group-hover:gap-3 transition-all">
            Sobre nosotros
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>
      
      <!-- Estadística destacada -->
      <div class="mt-12 text-center">
        <div class="inline-flex items-center gap-3 bg-blue-600 text-white px-8 py-4 rounded-2xl shadow-xl">
          <i class="fa-solid fa-users text-3xl"></i>
          <div>
            <div class="text-3xl font-bold">+500</div>
            <div class="text-sm font-semibold text-blue-100">Vidas transformadas</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <style>
    /* Estilos del carrusel */
    .carousel-item {
      opacity: 0;
      transition: opacity 1s ease-in-out;
      pointer-events: none;
    }
    
    .carousel-item.active {
      opacity: 1;
      pointer-events: auto;
    }

    .carousel-indicator {
      transition: all 0.3s ease;
    }

    .carousel-indicator.active {
      background-color: white !important;
      transform: scale(1.5);
    }

    @keyframes blob {
      0%, 100% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
    }

    @keyframes fade-in-up {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-blob {
      animation: blob 7s infinite;
    }

    .animation-delay-2000 {
      animation-delay: 2s;
    }

    .animation-delay-4000 {
      animation-delay: 4s;
    }

    .animate-fade-in-up {
      animation: fade-in-up 0.8s ease-out backwards;
    }

    .animation-delay-200 {
      animation-delay: 0.2s;
    }

    .animation-delay-400 {
      animation-delay: 0.4s;
    }

    .animation-delay-600 {
      animation-delay: 0.6s;
    }

    .animation-delay-800 {
      animation-delay: 0.8s;
    }

    .program-card {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .program-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
  </style>

  <script>
    import { getNoticias } from '../lib/auth.ts';
    import type { Noticia } from '../lib/auth.ts';

    // Contador animado
    function animateCounter(element: HTMLElement) {
      const target = parseInt(element.getAttribute('data-target') || '0');
      const duration = 2000;
      const step = target / (duration / 16);
      let current = 0;

      const timer = setInterval(() => {
        current += step;
        if (current >= target) {
          element.textContent = target.toString();
          clearInterval(timer);
        } else {
          element.textContent = Math.floor(current).toString();
        }
      }, 16);
    }

    // Iniciar contadores cuando sean visibles
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const counters = document.querySelectorAll('.counter');
          counters.forEach(counter => animateCounter(counter as HTMLElement));
          observer.disconnect();
        }
      });
    });

    const statsSection = document.querySelector('.counter');
    if (statsSection) {
      observer.observe(statsSection);
    }

    // Cargar últimas 3 noticias
    async function loadLatestNews() {
      try {
        const noticias = await getNoticias();
        const container = document.getElementById('noticias-preview');
        
        if (!container) return;
        
        const latest = noticias.slice(0, 3);
        
        if (latest.length === 0) {
          container.innerHTML = '<p class="text-slate-600 col-span-full text-center py-12">No hay noticias disponibles.</p>';
          return;
        }

        container.innerHTML = latest.map((noticia: Noticia) => `
          <article class="bg-white border-2 border-slate-100 rounded-xl p-6 hover:border-blue-200 hover:shadow-lg transition-all duration-300 group">
            <div class="text-xs text-slate-500 mb-2">
              ${new Date(noticia.fecha).toLocaleDateString('es-CO', { year: 'numeric', month: 'long', day: 'numeric' })}
            </div>
            <h3 class="font-bold text-lg mb-2 capitalize group-hover:text-blue-700 transition-colors">
              ${noticia.titulo}
            </h3>
            <p class="text-slate-600 text-sm mb-4 line-clamp-2">
              ${noticia.contenido}
            </p>
            <a href="/noticias" class="inline-flex items-center gap-2 text-blue-700 font-semibold text-sm group-hover:gap-3 transition-all">
              Leer más
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </a>
          </article>
        `).join('');
      } catch (error) {
        const container = document.getElementById('noticias-preview');
        if (container) {
          container.innerHTML = '<p class="text-slate-600 col-span-full text-center py-12">Error al cargar noticias.</p>';
        }
      }
    }

    // Cargar noticias al cargar la página
    loadLatestNews();

    // Carrusel Hero
    let currentSlide = 0;
    const slides = document.querySelectorAll('.carousel-item');
    const indicators = document.querySelectorAll('.carousel-indicator');
    const totalSlides = slides.length;

    function showSlide(index: number) {
      // Remover clase active de todos
      slides.forEach(slide => slide.classList.remove('active'));
      indicators.forEach(ind => ind.classList.remove('active'));
      
      // Agregar clase active al slide e indicador actual
      if (slides[index]) slides[index].classList.add('active');
      if (indicators[index]) indicators[index].classList.add('active');
      
      currentSlide = index;
    }

    function nextSlide() {
      const next = (currentSlide + 1) % totalSlides;
      showSlide(next);
    }

    function prevSlide() {
      const prev = (currentSlide - 1 + totalSlides) % totalSlides;
      showSlide(prev);
    }

    // Event listeners para botones
    const prevBtn = document.getElementById('prev-btn');
    const nextBtn = document.getElementById('next-btn');
    
    if (prevBtn) prevBtn.addEventListener('click', prevSlide);
    if (nextBtn) nextBtn.addEventListener('click', nextSlide);

    // Event listeners para indicadores
    indicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => showSlide(index));
    });

    // Auto-play cada 5 segundos
    setInterval(nextSlide, 5000);

    // Inicializar mostrando el primer slide
    showSlide(0);
  </script>
</Layout>
